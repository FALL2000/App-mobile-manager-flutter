import 'package:flutter/material.dart';
    
class MaAgentsPage extends StatelessWidget {

  const MaAgentsPage({ Key? key }) : super(key: key);
  
  @override
  Widget build(BuildContext context) {
     return Scaffold(
      body: Container(
        child: Text('Agents'),
      ),
    );
  }
}